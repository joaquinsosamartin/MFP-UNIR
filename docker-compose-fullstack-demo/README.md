# Angular + API + MySQL con Docker Compose

Entorno completo con frontend, API Node y MySQL persistente usando Compose.

## Ejecutar

```bash
cp .env.example .env
docker compose up -d --build
```

## URLs

- Frontend: http://localhost:8080
- API health: http://localhost:3000/health
